function _trash_help
    echo "Usage: trash <files>  Move files to the Trash"
    echo "Options:"
    echo "       -l, --list     List files in the Trash"
    echo "       -e, --empty    Empty the Trash"
    echo "       -v, --verbose  Show files being trashed"
    echo "       -h, --help     Show help message"
end
